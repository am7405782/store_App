import 'package:flutter/material.dart';

class OnbordingScrren extends StatelessWidget {
  const OnbordingScrren({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
